#include <iostream>
//#include<bits\c++.h>
using namespace std;
class data{
      int key,
      string value
  }
int main()
{
  //take in data
  //take the first value from arr and find the key and store it's corresponding value in string+"|". Do the same for all
  //the values.
  //using find funtion we can find and count the values in the data.txt
  //print all 
  int arr[4];
  for(int i=0;i<4;i++){
      cin>>arr[i];
  }
  //new d=new data()
  
  string s;int c=0;
  for(int i=0;i<4;i++){
      
      if(s.size()==0) s=data[0].value+"|";
      else s=s+data[i].value+"|";
      
  }
  
  return 0;
}
